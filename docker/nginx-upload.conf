# Increase upload limit to 20 MB for image submissions.
# Include this file inside the server {} block, or place it in conf.d/
# Deploy script copies it to /etc/nginx/conf.d/campus-upload.conf automatically.
client_max_body_size 20M;
